import { StyleSheet } from "react-native";
import { OPENSANS_REGULAR } from "./font";

const GlobalStyles = StyleSheet.create({
  globalFont: {
    fontFamily: OPENSANS_REGULAR,
  },
});

export default GlobalStyles;
